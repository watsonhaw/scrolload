export const paths = {
    root: '/',
    source: {
        root: './src/',
    },
    dist: {
        root: './dist/',
    },
};

export default {
	fileName: 'scrolload',
	moduleName: 'Scrolload', // var Scrolload = (function () { ...
	moduleId: 'Scrolload', // define(['Scrolload'], ...
}
